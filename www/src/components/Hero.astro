---
import {getImage} from 'astro:assets';
import geoEngineTitle from '../images/2301_GeoEngine_Titelbild_01.png';

const optimizedGeoEngineTitle = await getImage({src: geoEngineTitle});
const geoEngineBackground = 'url(' + optimizedGeoEngineTitle.src + ')';
---

<style define:vars={{geoEngineBackground}}>
    .hero-bg {
        background-image: var(--geoEngineBackground);
    }
</style>

<!-- Hero -->
<div class="relative overflow-hidden">
    <div class="max-w-[85rem] mx-auto px-4 sm:px-6 lg:px-8 py-10">
        <div class="max-w-2xl text-center mx-auto">
            <h1 class="block text-3xl font-bold text-gray-800 sm:text-4xl md:text-5xl dark:text-white">
                Spatio-Temporal <span class="text-main-green">Geodata</span> Processing
            </h1>
            <p class="mt-3 text-lg text-gray-800 dark:text-neutral-400">Connect your data. Gain insights.</p>
        </div>

        <div class="mt-10 relative max-w-5xl mx-auto">
            <div class="hero-bg w-full object-cover h-96 sm:h-120 bg-no-repeat bg-left bg-cover rounded-xl"></div>

            <div
                class="absolute bottom-12 -start-20 -z-1 size-48 bg-linear-to-b from-orange-500 to-white p-px rounded-lg dark:to-neutral-900"
            >
                <div class="bg-white size-48 rounded-lg dark:bg-neutral-900"></div>
            </div>

            <div class="absolute -top-12 -end-20 -z-1 size-48 bg-linear-to-t from-blue-600 to-cyan-400 p-px rounded-full">
                <div class="bg-white size-48 rounded-full dark:bg-neutral-900"></div>
            </div>
        </div>
    </div>
</div>
<!-- End Hero -->
