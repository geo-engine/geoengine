/*
 * Geo Engine API
 *
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * Contact: dev@geoengine.de
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ColumnRangeFilter : The `ColumnRangeFilter` operator allows filtering `FeatureCollection`s. Users can define one or more data ranges for a column in the data table that is then filtered. The filter can be used for numerical as well as textual columns. Each range is inclusive, i.e. `[start, end]` includes both the `start` and the `end` values.  ## Errors  If the value in the `column` parameter is not a column of the feature collection, an error is thrown.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ColumnRangeFilter {
    #[serde(rename = "type")]
    pub r#type: Type,
    #[serde(rename = "params")]
    pub params: Box<models::ColumnRangeFilterParameters>,
    #[serde(rename = "sources")]
    pub sources: Box<models::SingleVectorSource>,
}

impl ColumnRangeFilter {
    /// The `ColumnRangeFilter` operator allows filtering `FeatureCollection`s. Users can define one or more data ranges for a column in the data table that is then filtered. The filter can be used for numerical as well as textual columns. Each range is inclusive, i.e. `[start, end]` includes both the `start` and the `end` values.  ## Errors  If the value in the `column` parameter is not a column of the feature collection, an error is thrown.
    pub fn new(r#type: Type, params: models::ColumnRangeFilterParameters, sources: models::SingleVectorSource) -> ColumnRangeFilter {
        ColumnRangeFilter {
            r#type,
            params: Box::new(params),
            sources: Box::new(sources),
        }
    }
}
/// 
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum Type {
    #[serde(rename = "ColumnRangeFilter")]
    ColumnRangeFilter,
}

impl Default for Type {
    fn default() -> Type {
        Self::ColumnRangeFilter
    }
}

