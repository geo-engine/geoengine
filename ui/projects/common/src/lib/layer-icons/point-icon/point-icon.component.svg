<svg width="100%" height="100%" viewBox="0,0,24,24" xmlns="http://www.w3.org/2000/svg">
    <circle
        [style.stroke-width]="iconStyle().strokeWidth"
        [style.stroke-dasharray]="[]"
        [style.stroke]="iconStyle().strokeRGBA.rgbaCssString()"
        [style.fill]="iconStyle().fillRGBA.rgbaCssString()"
        cx="12"
        cy="12"
        r="8"
        id="point"
    />
</svg>
